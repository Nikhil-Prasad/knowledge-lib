# Environment variables for Docker Postgres (example)
# Copy to infra/docker/.env and customize for your local dev
# cp infra/docker/.env.example infra/docker/.env

POSTGRES_USER=kl
POSTGRES_PASSWORD=change-me
POSTGRES_DB=knowledge
POSTGRES_PORT=5432
