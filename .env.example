# Root application environment (example)
# Copy to .env for local development:
# cp .env.example .env
#
# Alembic and the app both read DATABASE_URL. We also provide PG* for convenience.

# SQLAlchemy/Alembic URL
DATABASE_URL=postgresql+psycopg2://kl:klpass@localhost:5432/knowledge

# Optional: Postgres discrete vars (helpful for psql and some tooling)
PGHOST=localhost
PGPORT=5432
PGUSER=kl
PGPASSWORD=klpass
PGDATABASE=knowledge
